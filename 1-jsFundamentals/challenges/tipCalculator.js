

/*

Make a tip calculator using a function
That take in the BILL (number)
Have it RETURN the TIP (number)
Capture that returned TIP in a VARIABLE 
Print that VARIABLE 

*/

let tip = (total) => {
    return total * .2;
    }

let tipAmount = tip (50);

console.log(tipAmount);

