// Equality

    // equal operator 
    "3"  == 3; // -> true (double equal sign evaluates the value only)
    
    // strict equality operator 
    "3" === 3; // -> false because the value is the same but the TYPE is not the same !! 9 times out of 10 use triple equal

// Opposite operator (Bang Operator)
    // NOT, or Bang, or Exclamaiton point
    "3" != 3; // -> false (not equal)

    "3" !== 3; // -> true (not strictly equal - this flips it with the bang operator)

// Greater than

    3 > 4; // -> false 


// Less than

    3 > 4; // -> true 


// Greater than or equal to

    3 >= 4;// -> false 


// Less than or equal to 

    3 <= 4; // - true

// AND operator 

() && () // the left side and the right side has to be true, to get true 

let x = 13;

x >=5 $$ x <= 33;


// OR operator , two pipes
() || ()

x > 3 || x == -3;